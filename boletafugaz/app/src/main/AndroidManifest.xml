<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.boletafugaz">

    <uses-permission android:name="android.permission.BLUETOOTH" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BoletaFugaz.NoActionBar">
        <activity
            android:name=".RegistrarGiroActivity"
            android:label="@string/title_activity_registrar_giro"
            android:theme="@style/Theme.BoletaFugaz.NoActionBar.NoActionBar"></activity>
        <activity android:name=".MostrarEmisorActivity" />
        <activity android:name=".FacturaActivity" />
        <activity android:name=".GenerarQRActivity" />
        <activity android:name=".RegistrarEmisorActivity" />
        <activity android:name=".ListaBluetoothActivity" />
        <activity
            android:name=".ProfileActivity"
            android:label="@string/title_activity_profile"
            android:theme="@style/Theme.BoletaFugaz.NoActionBar" />
        <activity android:name=".ResetPasswordActivity" />
        <activity android:name=".InicioActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".CalculadoraActivity" />
        <activity android:name=".SignUpActivity" />
        <activity android:name=".LoginActivity" />
    </application>

</manifest>